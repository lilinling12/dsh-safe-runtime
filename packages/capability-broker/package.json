{
  "name": "@dsh-safe/capability-broker",
  "version": "0.1.0-alpha.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @dsh-safe/policy-engine build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@dsh-safe/policy-engine": "workspace:*",
    "@dsh-safe/protocol": "workspace:*"
  }
}
